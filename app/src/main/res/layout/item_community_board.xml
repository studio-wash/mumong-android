<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="item"
            type="com.studiowash.mumong.community.FavoriteBoardItem" />
        <variable
            name="isFirstItem"
            type="boolean" />
        <variable
            name="isSelected"
            type="boolean" />
    </data>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingVertical="10dp">
        <View
            android:id="@+id/separator_line"
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:background="@drawable/gradient_mumong_brand_vertical"
            goneUnless="@{!isFirstItem}"/>
        <View
            android:id="@+id/start_margin_view"
            android:layout_width="@dimen/community_board_padding_horizontal"
            android:layout_height="match_parent"
            goneUnless="@{!isFirstItem}"/>
        <TextView
            android:id="@+id/bg_friend_image_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{item.boardName}"
            android:textColor="@{isSelected ? @color/text_black: @color/text_gray}"
            android:textSize="18sp"/>
        <View
            android:id="@+id/end_margin_view"
            android:layout_width="@dimen/community_board_padding_horizontal"
            android:layout_height="match_parent"/>
    </LinearLayout>
</layout>