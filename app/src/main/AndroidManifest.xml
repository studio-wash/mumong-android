<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studiowash.mumong">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuMong">
        <activity
            android:name="com.studiowash.mumong.presentation.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.studiowash.mumong.presentation.activity.community.article.CommunityArticleActivity" />
        <activity android:name="com.studiowash.mumong.presentation.activity.social.article.SocialArticleActivity" />
        <activity android:name="com.studiowash.mumong.presentation.activity.profile.ProfileActivity" />
        <activity android:name="com.studiowash.mumong.presentation.activity.practice.addpractice.AddPracticeActivity" />
    </application>

</manifest>